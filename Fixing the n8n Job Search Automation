Current Status:
The workflow is "successfully" executing, but the Google Sheets node is appending rows with empty data for the Title field, and the data may be appearing in the wrong tab or at the very bottom of the spreadsheet.

Step 1: Fix the Data Mapping Syntax
The current absolute reference in the Google Sheets node ({{ $node["Set — Format Job Fields"].json["job-title"] }}) is returning an empty value (==).

Action: Change the expression for the Title field to the relative version: {{ $json["job-title"] }}.

Why: This ensures n8n pulls directly from the data currently "in flight" from the node immediately preceding it.

Step 2: Sync the Tab Name
In n8n, the node is set to write to a tab named Job Search.

Action: Verify the tab name at the bottom of the Google Sheet. If the tab is named Jobs_SEARCH or Job_SEARCH (with an underscore), the n8n dropdown must be updated to match that exact name.

Step 3: Verify the "Set" Node Logic
The Output panel shows the Title as ==, which indicates the Set — Format Job Fields node is failing to extract the job title from the Gmail subject line.

Action: Check the expression in the Set node: {{ $json.subject.replace(/Job Alert:/gi, '').trim() }}.

Test: Ensure the test email subject line actually contains the phrase "Job Alert:". If the subject is just "Test," the expression will return nothing.

Step 4: Reset the Google Sheet Rows
n8n appends data to the first truly empty row at the bottom of the document.

Action: In the Google Sheet, select all rows from Row 2 to the very bottom (Row 1000+), right-click, and select "Delete Rows".

Goal: This prevents data from being "hidden" hundreds of rows down due to previous failed tests or accidental formatting.

Step 5: Final Execution Test
Click "Execute Workflow" in n8n first to put the agent in "waiting" mode.

Send a fresh test email with a subject line like: Job Alert: Assistant Editor NYC Test.

Confirm the Google Sheets node output shows the title text instead of ==.
